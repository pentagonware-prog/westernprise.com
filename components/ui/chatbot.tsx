"use client";
import { useState } from "react";

export function Chatbot(){const[open,setOpen]=useState(false);const[message,setMessage]=useState("");const[sent,setSent]=useState(false);return <div className="chatbot"><button className="chatbot-launcher" onClick={()=>setOpen(!open)} aria-label={open?"Close Westernprise assistant":"Open Westernprise assistant"}>{open?<b>×</b>:<span className="message-icon"/>}</button>{open&&<div className="chatbot-panel"><div className="chatbot-head"><div><b>Westernprise Assistant</b><small>Usually replies instantly</small></div><i/></div><div className="chatbot-body"><div className="bot-message">Hi, how can we help you understand Westernprise?</div>{sent&&<div className="user-message">{message}</div>}<div className="chatbot-chips"><button onClick={()=>setMessage("I’d like to book a demo")}>Book a demo</button><button onClick={()=>setMessage("Tell me about confidential workspaces")}>Confidential workspace</button></div></div><form onSubmit={e=>{e.preventDefault();if(message.trim())setSent(true)}}><input value={message} onChange={e=>{setMessage(e.target.value);setSent(false)}} placeholder="Ask a question…"/><button aria-label="Send message">→</button></form></div>}</div>}
